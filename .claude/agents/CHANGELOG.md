# .claude/agents CHANGELOG

## content-designer v1 → v2 (2026-03-09)

### 변경 파일
- `content-designer.v1.md` (v1, 보존)
- `content-designer.md` (v2, 덮어쓰기)

### 변경 사유 (토큰 효율 + 명확성)

| # | 분류 | 내용 |
|---|------|------|
| 1 | 토큰 절감 | Step 3을 Step 2의 반복 서술에서 "차이점만 기술" 방식으로 리팩터링 (~400토큰) |
| 2 | 토큰 절감 | 필수 필드 체크리스트 표 제거 — JSON 스키마와 중복 (~180토큰) |
| 3 | 스키마 명확화 | `seed_h2_link` 기본값을 JSON null로 명시 (string "null" 혼용 방지) |
| 4 | 명확성 | `trend_direction` 폴백 기준: "가장 대표적인" → "average가 가장 높은" |
| 5 | 엣지케이스 | `fan_outs: []`, naver_trend 0, 필드 누락, top_competitors 없음 처리 추가 |
| 6 | 명확성 | `expansion_role` 다양성 기준 수치화: 70% 초과 시 재검토 |
| 7 | 구조 개선 | 역할 정의 1문장 압축, "핵심 원칙" 섹션 제거 (절차에서 중복) |
| 8 | 구조 개선 | 제약 조건 섹션 신설, 주의사항을 긍정형 제약+엣지케이스로 재구성 |

### 토큰 추정
- v1: ~2,600토큰
- v2: ~1,950토큰 (약 630토큰 절감)

---

## researcher v1 → v2 (2026-03-09)

### 변경 파일
- `researcher.md` (v1, 보존)
- `researcher_v2.md` (v2, 신규)

### 변경 사유 (토큰 효율 + 중복 제거)

| # | 분류 | 내용 |
|---|------|------|
| 1 | 중복 제거 | "상위 개념 금지" 규칙 5개소 → 핵심 원칙 1개소로 통합 |
| 2 | 중복 제거 | WebFetch 재시도 규칙 3개소 → 핵심 원칙 1개소로 통합 |
| 3 | 중복 제거 | GEO 병렬/순차 호출 규칙 → 주의사항 1개소로 통합 (단계 2-5에서 제거) |
| 4 | 토큰 절감 | 도구 출력 JSON 스키마 제거 (tools 1~5) — 호출 형식만 표로 압축 |
| 5 | 토큰 절감 | 필수 필드 요약 테이블(472~501행) 삭제 — JSON 스키마와 중복 |
| 6 | 스키마 명확화 | fan_outs trend에 series 없는 이유 명시 (LIGHT 수집) |
| 7 | 엣지케이스 | 빈 입력 / 비질문 형태 처리 추가 |
| 8 | 버전 관리 | frontmatter에 `version: 2` 추가 |
| 9 | 구조 개선 | "병렬 실행" 원칙을 핵심 원칙 상단에 명시 |

### 토큰 추정
- v1: ~4,500~5,000 토큰
- v2: ~2,800~3,200 토큰
- 절감: ~35~40%
